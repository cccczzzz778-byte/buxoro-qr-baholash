const $=s=>document.querySelector(s);
const form=$('#loginForm'),button=$('#loginButton'),error=$('#loginError'),password=$('#password'),toggle=$('#togglePassword');
toggle.addEventListener('click',()=>{const show=password.type==='password';password.type=show?'text':'password';toggle.textContent=show?'Yashirish':'Ko‘rsatish';toggle.setAttribute('aria-label',show?'Parolni yashirish':'Parolni ko‘rsatish')});
form.addEventListener('submit',async e=>{e.preventDefault();error.classList.add('hidden');button.disabled=true;const old=button.textContent;button.innerHTML='<span class="pro-submit-spinner"></span>Kirilmoqda...';const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),15000);try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:$('#username').value.trim(),password:password.value,remember:$('#remember').checked}),signal:controller.signal});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Kirishda xatolik.');sessionStorage.setItem('csrf',j.csrf||'');location.replace(j.next||(j.role==='district'?'/analytics.html':'/dashboard'))}catch(ex){error.textContent=ex.name==='AbortError'?'Server javobi kechikdi. Qayta urinib ko‘ring.':ex.message;error.classList.remove('hidden');password.focus()}finally{clearTimeout(timer);button.disabled=false;button.textContent=old}});
