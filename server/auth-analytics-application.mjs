import { database, sql } from './database.mjs';
import { randomToken, digest, equal, secret, sign, verifyPassword, clientToken, csrfFor, requestOrigin, publicBase, cookie } from './security.mjs';
import { securityHeaders } from './application.mjs';

const districts=['Buxoro shahri','Kogon shahri','Buxoro tumani','Vobkent tumani','G‘ijduvon tumani','Jondor tumani','Kogon tumani','Olot tumani','Peshku tumani','Qorako‘l tumani','Qorovulbozor tumani','Romitan tumani','Shofirkon tumani'];
const usernamePattern=/^[a-z0-9._-]{3,40}$/; const districtUserPrefix='district_user:';
const ADMIN_FALLBACK_HASH='scrypt:3a17b69fe0c364393f332e1fce179b73:fb46970f63b3f811fcba750d2133c3bde77aa95e78469fc5ce58767e46d26689e6e7fd53459636a79b11fc9cf10c7c02ef7ed0b5f80b3138bb1f9cb7d5409692';
const MANAGEMENT_FALLBACK_HASH='scrypt:41dec5496434351ac0894f0df0703dba:f5775dd23fdee02360a376f859531530e188494ad27fc15c0fa2c5b5b0844aa049e30b9b735f3f9a8b0ceca2223bf54100116b071448564ee6f4274bf01e46cd';
class HttpError extends Error{constructor(status,message){super(message);this.status=status}} const fail=(s,m)=>{throw new HttpError(s,m)}; const isoNow=()=>new Date().toISOString();
function json(res,status,value){res.statusCode=status;res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store');res.end(JSON.stringify(value))}
async function body(req){if(!/^application\/json\b/i.test(req.headers['content-type']||''))fail(415,'Ma’lumot JSON shaklida yuborilishi kerak.');const chunks=[];let size=0;for await(const chunk of req){size+=chunk.length;if(size>16384)fail(413,'Yuborilgan ma’lumot juda katta.');chunks.push(chunk)}try{const v=JSON.parse(Buffer.concat(chunks).toString('utf8'));if(!v||Array.isArray(v)||typeof v!=='object')fail(400,'Ma’lumot shakli noto‘g‘ri.');return v}catch(e){if(e instanceof HttpError)throw e;fail(400,'Ma’lumot shakli noto‘g‘ri.')}}
function field(v,label,min,max){if(typeof v!=='string')fail(400,`${label}ni to‘g‘ri kiriting.`);const clean=v.normalize('NFKC').trim().replace(/[\u0000-\u0008\u000b\u000c\u000e-\u001f]/g,'');if(clean.length<min||clean.length>max)fail(400,`${label}: ${min}–${max} ta belgi bo‘lishi kerak.`);return clean}
function makeSessionToken(scope){const nonce=randomToken(),payload=Buffer.from(JSON.stringify(scope)).toString('base64url'),signature=sign(`session:${nonce}.${payload}`);return`${nonce}.${payload}.${signature}`}
function parseSessionToken(token){const parts=String(token||'').split('.');if(parts.length!==3)return null;const[nonce,payload,signature]=parts;if(!nonce||!payload||!equal(signature,sign(`session:${nonce}.${payload}`)))return null;try{const d=JSON.parse(Buffer.from(payload,'base64url').toString('utf8'));if(d.role==='admin')return{role:'admin',district:'',username:String(d.username||process.env.ADMIN_USERNAME||'admin')};if(d.role==='management'&&usernamePattern.test(String(d.username||'')))return{role:'management',district:'',username:d.username};if(d.role==='district'&&districts.includes(d.district)&&usernamePattern.test(String(d.username||'')))return{role:'district',district:d.district,username:d.username}}catch{}return null}
async function session(db,req){const token=clientToken(req);if(!token||token.length>512)fail(401,'Tizimga qayta kiring.');const[row]=await db.all(sql`SELECT token_hash FROM admin_sessions WHERE token_hash=${digest(token)} AND expires_at>${isoNow()}`);if(!row)fail(401,'Tizimga qayta kiring.');const scope=parseSessionToken(token);if(!scope)fail(401,'Tizimga qayta kiring.');return{...scope,token}}
async function rateLimit(db,req){const remote=String(req.headers['x-forwarded-for']||'').split(',')[0].trim()||req.socket?.remoteAddress||'local',bucket=Math.floor(Date.now()/(15*60000)),key=sign(`login:${remote}:${bucket}`),expires=new Date((bucket+1)*15*60000).toISOString();const[row]=await db.all(sql`INSERT INTO rate_limits (key,hits,expires_at) VALUES (${key},1,${expires}) ON CONFLICT (key) DO UPDATE SET hits=rate_limits.hits+1 RETURNING hits`);if(Number(row.hits)>20)fail(429,'Juda ko‘p urinish bo‘ldi. Birozdan keyin qayta urinib ko‘ring.')}
function guardOrigin(req){const origin=req.headers.origin;if(origin&&![requestOrigin(req),publicBase(req)].includes(origin))fail(403,'Bu manzildan so‘rov yuborish mumkin emas.');if(req.headers['sec-fetch-site']==='cross-site')fail(403,'So‘rov manzili tasdiqlanmadi.')}
function presentInstitution(row,req){return{id:row.id,name:row.name,district:row.district,address:row.address,active:Boolean(row.active),createdAt:row.created_at,feedbackUrl:`${publicBase(req)}/?m=${encodeURIComponent(row.id)}`,qrUrl:`/api/qr/${encodeURIComponent(row.id)}.png`,posterUrl:`/poster/${encodeURIComponent(row.id)}`}}

export default async function authAnalyticsApplication(req,res){securityHeaders(res);res.setHeader('Cache-Control','no-store');try{secret();const url=new URL(req.url,requestOrigin(req)),route=url.pathname.replace(/^\/api\/?/,'').replace(/^\/+|\/+$/g,''),method=req.method,db=await database();
if(route==='auth/login'&&method==='POST'){guardOrigin(req);await rateLimit(db,req);const data=await body(req),username=field(data.username,'Login',1,80).toLowerCase(),password=field(data.password,'Parol',1,256);let scope=null;const[adminSetting]=await db.all(sql`SELECT value FROM app_settings WHERE key='admin_password'`),adminUsername=String(process.env.ADMIN_USERNAME||'admin').toLowerCase(),adminEnv=String(process.env.ADMIN_ACCESS_PASSWORD||'');const adminOk=(adminEnv&&equal(password,adminEnv))||await verifyPassword(password,adminSetting?.value)||await verifyPassword(password,ADMIN_FALLBACK_HASH);if(equal(username,adminUsername)&&adminOk)scope={role:'admin',district:'',username:process.env.ADMIN_USERNAME||'admin'};
if(!scope&&username==='boshqarma'){const[managementSetting]=await db.all(sql`SELECT value FROM app_settings WHERE key='management_password'`),managementEnv=String(process.env.BOSHQARMA_PASSWORD||'');const managementOk=(managementEnv&&equal(password,managementEnv))||await verifyPassword(password,managementSetting?.value)||await verifyPassword(password,MANAGEMENT_FALLBACK_HASH);if(managementOk)scope={role:'management',district:'',username:'boshqarma'}}
if(!scope&&usernamePattern.test(username)){const[saved]=await db.all(sql`SELECT value FROM app_settings WHERE key=${districtUserPrefix+username}`);if(saved?.value){try{const user=JSON.parse(saved.value);if(user.active!==false&&districts.includes(user.district)&&await verifyPassword(password,user.passwordHash))scope={role:'district',district:user.district,username:user.username}}catch{}}}
if(!scope)fail(401,'Login yoki parol noto‘g‘ri.');const token=makeSessionToken(scope),seconds=data.remember===true?7*86400:8*3600;await db.all(sql`INSERT INTO admin_sessions (token_hash,expires_at) VALUES (${digest(token)},${new Date(Date.now()+seconds*1000).toISOString()})`);res.setHeader('Set-Cookie',cookie(req,token,seconds));return json(res,200,{...scope,csrf:csrfFor(token),next:scope.role==='admin'?'/dashboard':'/analytics.html'})}
if(route==='auth/session'&&method==='GET'){const auth=await session(db,req);return json(res,200,{username:auth.username,role:auth.role,district:auth.district,csrf:csrfFor(auth.token),publicBaseUrl:publicBase(req)})}
if(route==='analytics/institutions'&&method==='GET'){const auth=await session(db,req),rows=auth.role==='district'?await db.all(sql`SELECT * FROM institutions WHERE district=${auth.district} ORDER BY active DESC,name`):await db.all(sql`SELECT * FROM institutions ORDER BY active DESC,name`);return json(res,200,{scope:{role:auth.role,district:auth.district},institutions:rows.map(r=>presentInstitution(r,req))})}
if(route==='analytics/feedback'&&method==='GET'){const auth=await session(db,req),rows=auth.role==='district'?await db.all(sql`SELECT f.id,f.institution_id,f.rating,f.is_complaint,f.day,f.created_at FROM feedbacks f JOIN institutions i ON i.id=f.institution_id WHERE i.district=${auth.district} ORDER BY f.created_at DESC`):await db.all(sql`SELECT f.id,f.institution_id,f.rating,f.is_complaint,f.day,f.created_at FROM feedbacks f ORDER BY f.created_at DESC`);return json(res,200,{scope:{role:auth.role,district:auth.district},feedback:rows})}
fail(404,'Sahifa topilmadi.')}catch(error){const status=error instanceof HttpError?error.status:500;if(status>=500)console.error(error);return json(res,status,{error:status>=500?'Server bilan bog‘lanishda xatolik.':error.message})}}